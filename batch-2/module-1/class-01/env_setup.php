<?PHP
//==========TOPIC=========
//variable
//variable type
//============variable scope=================
///--local scope:Variables declared within a function are only accessible within that function.
///--global scope:Variables declared outside of any function can be accessed anywhere in the script.
///--super global:Special global arrays like $_GET, $_POST, $_SESSION, etc., which are accessible from anywhere in the script.
//data type
//constant define
//==================printf=====================
//PHP, the printf function is used for formatted output. It allows you to print formatted strings with placeholders that can be replaced by values. like: 
    //$name = "Alice";
    // $age = 25;
    // printf("Name: %s, Age: %d", $name, $age);

    //======placeholder type
    //%s -> string
    //%d -> decimal value
    //%i -> integer
    //%f -> float
    //%x ->  lowercase hexadecimal numbers 
    //%X -> Uppercase hexadecimal numbers



//sprintf

///////////////ALHAMDULILLH!!!! all are done///////////////